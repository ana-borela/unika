const scroller = new LocomotiveScroll({
  el: document.querySelector('[data-scroll-container]'),
  smooth: true
});


/*Progress Bar

$(document).ready(function () {

  let Sistemas = document.getElementById('sistemas')
  let sistemas = new ProgressBar.Circle(Sistemas, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Desenvolvimento de sistemas'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Websites = document.getElementById('websites')
  let websites = new ProgressBar.Circle(Websites, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Desenvolvimento de websites'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Consultoria = document.getElementById('consultoria')
  let consultoria = new ProgressBar.Circle(Consultoria, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Consultoria em TI'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Redes = document.getElementById('redes')
  let redes = new ProgressBar.Circle(Redes, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Redes & Conectividade'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Manutencao = document.getElementById('manutencao')
  let manutencao = new ProgressBar.Circle(Manutencao, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Manutenção de servidores'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Assistencia = document.getElementById('assistencia')
  let assistencia = new ProgressBar.Circle(Assistencia, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Assistência técnica'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Backup = document.getElementById('backup')
  let backup = new ProgressBar.Circle(Backup, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Soluções de Backup'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  let Helpdesk = document.getElementById('helpdesk')
  let helpdesk = new ProgressBar.Circle(Helpdesk, {

      color: '#fff',
      strokeWidth: 0.5,
      duration: 6000,
      text: { value: 'Helpdesk Local'},
      from: { color: '#0a0096' },
      to: { color: '#cefcff'},

      step: function(state, circle) {

          circle.path.setAttribute('stroke', state.color)
      }


  })

  sistemas.animate(1.0)
  websites.animate(1.0)
  consultoria.animate(1.0)
  redes.animate(1.0)
  manutencao.animate(1.0)
  assistencia.animate(1.0)
  backup.animate(1.0)
  helpdesk.animate(1.0)

})*/